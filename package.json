{
  "name": "2--mern",
  "version": "1.0.0",
  "description": "// npm init -y\r // npm install express\r // -> Para poder leer las cookies a traves de objetos de JavaScript.\r // Convierte la cabecera en una cookie\r // npm cookie-parser\r // -> comunicar que frontend pueden tener comunicación con nuestro servidor\r // npm install cors\r // -> Nos ayuda a ver por consola las peticiones que van llegando\r // npm install morgan\r // Driver de conexion con 'progrest'. Vamos a poder enviarle consultas sql al backend \r // npm install pg\r // -> nos ayuda a encriptar el correo para poder generar un avatar\r // npm install md5 \r // -> modulo para generar tokes desde backend y enviarlo al fronted y decirle que puede tener acceso a ese token\r // npm install jsonwebtoken\r // -> Es la forma de tener distintas rutas en el backend. Nos ayuda a manejar errores \r // npm install express-promise-router\r // ->\r // npm i bcrypt.js",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon .",
    "start": "node ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "express-promise-router": "^4.1.1",
    "jsonwebtoken": "^9.0.2",
    "md5": "^2.3.0",
    "morgan": "^1.10.0",
    "pg": "^8.13.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "eslint": "^9.11.1",
    "nodemon": "^3.1.7"
  }
}
